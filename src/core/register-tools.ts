import type { ToolRegistry } from "../tools/index";

/**
 * Register all built-in tools onto the given registry.
 *
 * Currently a no-op — built-in tools are not yet implemented.
 * This function will be populated as tools are added to `core/`.
 */
export function registerBuiltinTools(registry: ToolRegistry): void {
  // TODO: register built-in tools when implemented
}